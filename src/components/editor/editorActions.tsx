import React from 'react';
/**
 * Compatibility shim — the registry lives in lib/editor now.
 * All existing imports from components/editor/editorActions keep working.
 */
export * from '../../lib/editor/editorActions';
